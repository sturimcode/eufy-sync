#!/bin/bash
# Wrapper script for eufy-garmin-sync CLI
DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$DIR"
source .venv/bin/activate 2>/dev/null || { echo "Run ./setup.sh first"; exit 1; }
python -m eufy_garmin_sync.cli "$@"
